import unittest
import coverletter

class TestCoverLetter(TestCase):

  #Test that it re-runs until valid location
  def test_valid_location(self):
    assert 
  #Test that it overwrite function overwrites properly
  def
  #Test that replace text works
  
  #Test that file is saved

  #Test that it saves to current working directory when location is not valid

  #Test that it defaults to option 1 for separator
